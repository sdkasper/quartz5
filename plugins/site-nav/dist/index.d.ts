import { QuartzComponent } from '@quartz-community/types';

interface Options {
    maxPills: number;
}
declare const _default: (opts?: Partial<Options>) => QuartzComponent;

export { _default as SiteNav };
